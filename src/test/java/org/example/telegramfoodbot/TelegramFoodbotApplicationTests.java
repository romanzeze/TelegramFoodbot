package org.example.telegramfoodbot;

import org.junit.jupiter.api.Test;

class TelegramFoodbotApplicationTests {

    @Test
    void contextLoads() {
        // Integration context test requires a running DB and bot token.
        // Run with: docker compose up -d postgres && ./gradlew test
    }
}
